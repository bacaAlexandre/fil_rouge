{% extends 'base.html.twig' %}

{% block body %}
    <div class="accueil_regroupement">
        {% for val in data %}
            <div class="accueil_bloc_image">
                <img src="https://image.tmdb.org/t/p/w500{{ val.poster_path }}" alt="">
                <div>
                    <h4>{{ val.title }}</h4>
                    <p>{{ val.overview }}</p>
                    <form action="{{ path('page_film',{'id':val.id }) }}">
                        <button>Détail</button>
                    </form>
                </div>
            </div>

        {% endfor %}
    </div>


{% endblock %}
